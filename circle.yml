machine:
  node:
    version: 4.6.2
  pre:
    - curl https://install.meteor.com | /bin/sh
dependencies:
  override:
    - meteor npm install
    - meteor list
test:
  override:
    - meteor npm run lint